import 'package:flutter/material.dart';

class Securtiy extends StatelessWidget {
  const Securtiy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return   Scaffold(
      appBar:AppBar(
        title: Text("Securtiy"),
        centerTitle: true,
      ) ,
    );

  }
}
