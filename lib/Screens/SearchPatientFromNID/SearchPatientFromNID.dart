import 'package:flutter/material.dart';

class SearchFromNID extends StatelessWidget {
  const SearchFromNID({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: Column(
        children:
        [

        ],
      ),
    );
  }
}
